/**
 * Phone utilities — re-export from the canonical shared/ implementation.
 * All new code should import directly from '../../shared/phone'.
 */
export { normalizePhone, isValidPhone, formatPhone } from '../../shared/phone';
