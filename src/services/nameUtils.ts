/**
 * Name utilities — re-export from the canonical shared/ implementation.
 * All new code should import directly from '../../shared/name'.
 */
export { splitName, joinName, slugify, buildDisplayName } from '../../shared/name';
