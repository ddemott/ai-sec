# Build artifacts
dist/
.next/
coverage/
playwright-report/
test-results/

# Dependencies
node_modules/

# Generated files
package-lock.json
*.lock

# Vendored / static assets
public/

# DB migrations — hand-written SQL, don't reformat
supabase/migrations/

# Docs that have intentional formatting
RESOLVED.md
LESSONS_LEARNED.md
docs/CURRENT_STATUS_ARCHIVED_*.md

# Auth state for E2E (JSON snapshots)
dashboard/e2e/.auth/

# Self-signed certs
certs/

# External audit output (Grok watcher, 2026-09-03). Written into the repo root
# by a tool outside this codebase, mid-session, on its own schedule. It is not
# source and we do not own its formatting — but `format:check` globs `*.md` at
# the root, so without this entry every commit and push fails on a file that
# appeared while the hook was running.
GROK_FINDINGS-*.md
.grok-audit/
