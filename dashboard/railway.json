{
  "$schema": "https://railway.com/railway.schema.json",
  "//": "Commands run from the REPO ROOT (set the dashboard service's Root Directory to BLANK, not 'dashboard') so the build can see repo-root /shared/* that the dashboard imports. We cd into dashboard and use plain next build/start (no standalone — see next.config.mjs).",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd dashboard && npm ci && npm run build"
  },
  "deploy": {
    "startCommand": "cd dashboard && npx next start -H 0.0.0.0 -p $PORT",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
