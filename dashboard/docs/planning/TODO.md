# Pepper Documentation Backlog

Focus: documentation, handoffs, plain-language thoroughness. No code changes allowed in these passes.

Last hygiene: 2026-09-04

**Verified counts from real searches/terminal (no memory):**
- Migrations: 0 (dashboard package; backend separate)
- Components: 258
- Tests: 104
- Routes: 11 page routes (0 dedicated route.ts handlers)

**Live items only:**
- [ ] Maintain plain-language handoffs in all docs. Make every section readable for non-technical Pepper users. Update counts from fresh terminal/search_files runs only.
- [ ] Build dedicated voice-ai-testing handoff with full postmortem template, guardrails checklist, and exact test commands.
- [ ] Keep DIRECTORY_STRUCTURE.md, README tables, and all references in sync with live filesystem. No stale counts or paths.
- [ ] After any doc edit, verify zero new stale claims via search_files (old planning refs in e2e/ + components/ left untouched per no-code rule). Use terminal counts only.

All resolved narratives moved to RESOLVED.md. This file contains only live items.
