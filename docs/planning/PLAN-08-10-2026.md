# PLAN-08-10-2026: Initial Clarification, Service Descriptions, Filler vs Meaningful Interruptions, Scratchpad Auto-Fill, Non-Linear Mind-Change Handling

## Goal
Strengthen first-turn clarification so confused/vague openers get immediate "what I can do" + tenant service facts (from `greeting_menu` / roster — never a hardcoded person name). Distinguish filler noise ("yeah", "uh-huh", "k", "okay", "go on") from real interruptions ("what", "repeat that", "don't get it", "wait"). Let LLM decide response (repeat, simplify, deepen). Add scratchpad + lookahead so prior info auto-fills later nodes and mind-changes do not lose state. Support 0-1 message + 0-n services with service-in-message detection + appointment offer. All built on existing ChecklistTracker/trees without new architecture.

## Scope
- Only initial response, prompt rules, tracker state, trees updates.
- No new trees, no DB changes, no major agent rewrite.
- PRs follow PR-01.08-10-2026-(short-title).

## Dependencies & Priority (intelligent order)
1. Read current prompt builder, tracker, trees, detector (baseline).
2. Run existing tests (ChecklistAgent, tracker, sim-toolselect, sim-call) — establish green baseline.
3. Create PLAN-08-10-2026.md + prioritized TODO checklist (this file + todo tool).
4. Extend ChecklistTracker with scratchpad Map + lookahead pre-fill before context render (core state for auto-fill/mind-change).
5. Update buildChecklistPrompt with explicit capability paragraph + Dale service list + filler-vs-intent rules (highest impact on confused openers).
6. Add clarify_opener node to IDENTITY_TREE or initial purpose logic.
7. Add service-in-message detection hook in record_answer / checklistTools (triggers offer + add service tree).
8. Update prompt rules for non-linear pivots ("acknowledge change, update scratchpad, never re-ask known facts").
9. Add regression tests for filler ignore, meaningful clarification, mind-change auto-fill, service-in-message offer.
10. Run full test suite + sim-call with vague opener + interruption + pivot scenarios.
11. Create PR-01.08-10-2026-initial-clarification (prompt + opener).
12. Create PR-02.08-10-2026-scratchpad-autofill (tracker).
13. Create PR-03.08-10-2026-interruption-handling (rules + detection).
14. Update docs/planning/TODO.md + RESOLVED.md with this work.
15. Verify on local demo URL + production simulator.

## Exit Criteria per Phase
- Each PR: tests green before + after, no regression on existing trees, sim-call with vague opener succeeds with clarification, filler ignored, meaningful interruption handled intelligently, mind-change preserves prior answers.
- Final: wife’s production calls style improved, no re-ask of known info, service-in-message leads to offer.

## Risks
- Prompt bloat — keep under 2k chars added.
- Over-filtering real speech as filler — test with real audio sim.
- Scratchpad key collisions — enforce strict node_id discipline (already in trees).

Start execution with phase 1 (reads + tests). Use todo tool for checklist. All changes via patch after tests. PR naming strict.